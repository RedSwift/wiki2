<%= form_for (cartoon) do |f| %>
  <div>
  <%= f.label :title %> <br>
  <%= f.text_field :title %>
  <br>
  </div>

  <div>
  <%= f.label :content %> <br>
  <%= f.text_area :content %>
  <br>
  </div>

  <%= f.submit "Submit" %>

<% end %>
